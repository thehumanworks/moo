{
  "name": "@moo/mcp-server",
  "version": "0.5.20",
  "private": true,
  "type": "module",
  "bin": {
    "moo-mcp-server": "./src/main.ts"
  },
  "scripts": {
    "build": "bun build --compile src/main.ts --outfile ../../zig-out/bin/moo-mcp-server && sh -c 'if [ \"$(uname)\" = Darwin ]; then codesign --force --sign - ../../zig-out/bin/moo-mcp-server; fi'",
    "smoke": "bun run scripts/smoke.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@types/bun": "^1.3.5",
    "typescript": "^5.9.3"
  }
}
